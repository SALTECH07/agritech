# Farm Buddy development environment
# Copy this file to .env.docker for local Docker development:
#   cp .env.development.example .env.docker

# ---------- PostgreSQL ----------
POSTGRES_DB=farmbuddy_dev
POSTGRES_USER=farmbuddy
POSTGRES_PASSWORD=change_this_dev_database_password
POSTGRES_BIND_ADDRESS=127.0.0.1
POSTGRES_PORT=5432

# ---------- Flask backend ----------
BACKEND_BIND_ADDRESS=127.0.0.1
BACKEND_PORT=8000
JWT_SECRET=change_this_dev_jwt_secret_use_a_long_random_text
JWT_EXPIRES_HOURS=168
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ---------- React frontend ----------
FRONTEND_BIND_ADDRESS=127.0.0.1
FRONTEND_PORT=5173
VITE_API_BASE_URL=http://localhost:8000

# Temporary placeholders while the old frontend screens are migrated away from
# Supabase. Do not put real secrets in VITE_ variables.
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=placeholder

# ---------- AI assistant ----------
# Keep AI keys on the backend only. Never prefix real AI keys with VITE_.
AI_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest
GEMINI_TEMPERATURE=0.3

# Optional OpenAI fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
OPENAI_REASONING_EFFORT=low
OPENAI_TEXT_VERBOSITY=low

# ---------- Optional notifications ----------
TWILIO_API_KEY=
TWILIO_FROM_SMS=
TWILIO_FROM_WHATSAPP=
